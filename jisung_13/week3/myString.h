#include <stdio.h>
#include <stdlib.h>
int mStrlen(const char*);           // length of string
char* mStrcpy(const char*,char*);     // copy string
char* mStrncpy(const char*,char*,int); // copy string
char* mStrcat(char*,const char*);     // add string
int mStrcmp(const char*,const char*);      // compare string by dic order
const char* mStrchr(const char*,char);       // char searching
